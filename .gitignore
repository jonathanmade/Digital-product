# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Impeccable design skill — local/session state only; design.json (the
# DESIGN.md sidecar) stays tracked. The skill itself (incl. its platform
# binary) installs per-machine via `npx impeccable install`, not via git.
.claude/
.impeccable/config.local.json
.impeccable/hook.cache.json

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
